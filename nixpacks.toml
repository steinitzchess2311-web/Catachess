providers = ["python"]

[phases.setup]
nixPkgs = [
  "python311Full",
  "python311Packages.pip",
  "gcc",
  "pkg-config",
  "postgresql"
]

[phases.install]
cmds = [
  "python -m venv .venv",
  ". .venv/bin/activate && pip install -r requirements.txt"
]

[start]
cd backend && ../.venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port $PORT

