# 前端渲染与交互卡顿问题诊断（Patch 页面）

## 结论摘要

当前 `patch` 页面“点击延迟”和“渲染卡顿”是多因素叠加，主因不在单一组件，而是：

1. 章节点击后 **先等网络再更新选中态**，导致明显“点击无响应感”。
2. Study 状态层对树结构做 **高频深拷贝（JSON stringify/parse）**，主线程阻塞明显。
3. 代码内存在 **大量调试日志输出**，在开发模式（尤其 DevTools 打开）下显著拖慢交互。
4. Chapter 列表拖拽过程中 `dragover` 高频 `setState`，导致章节栏连续重渲染。
5. React `StrictMode` 在开发环境放大上述问题体感。

---

## 现象与证据

## 1) 章节点击延迟（高优先级）

- 触发点：`patch/sidebar/ChapterList.tsx:206`（点击章节调用 `onSelectChapter`）
- 调用链：`patch/PatchStudyPage.tsx:258` -> `loadChapterTree(...)`
- 关键行为：`patch/PatchStudyPage.tsx:211` 先 `await api.get(...)`，直到 `patch/PatchStudyPage.tsx:222` 才 `selectChapter(...)`
- 影响：章节高亮和内容切换要等接口返回；网络慢/重试时，用户体感为“点击没反应”
- 放大项：`patch/PatchStudyPage.tsx:237` 有重试（500ms，最多3次）

## 2) 状态更新重（高优先级）

`studyContext` 中多个动作都在做深拷贝：

- `patch/studyContext.tsx:124`（history snapshot）
- `patch/studyContext.tsx:251`（ADD_MOVE）
- `patch/studyContext.tsx:293`（SET_COMMENT）
- `patch/studyContext.tsx:310`（DELETE_MOVE）
- `patch/studyContext.tsx:354`（PROMOTE_VARIATION）

影响：

- 树数据稍大时，JSON 深拷贝会阻塞主线程。
- 这些动作又会触发上下文消费者重渲染，卡顿进一步放大。

## 3) 调试日志过多（高优先级）

在 `patch/` 下存在约 `260` 处 `console.*` 输出（统计值）。

典型高频路径：

- `patch/studyContext.tsx:554`（saveTree）
- `patch/studyContext.tsx:600`（auto-save effect）
- `patch/PatchStudyPage.tsx:212`（loadChapterTree）
- `patch/sidebar/components/AnalysisPanel.tsx:54`（render 时打印）

影响：

- Dev 模式 + 打开控制台时，日志 IO 和对象序列化成本很高。
- render 中打印会直接增加每次渲染耗时。

## 4) Chapters 栏拖拽时高频重渲染（中高优先级）

- `patch/sidebar/ChapterList.tsx:123`~`134`：`onDragOver` 每次鼠标移动都 `setDropTarget({ ... })`
- 新对象导致状态频繁变化，列表不断重渲染
- 与复杂样式（阴影/动画/过渡）叠加后，章节栏最容易出现“掉帧”

## 5) 开发环境放大效应（中优先级）

- `frontend/web/src/main.tsx:6` 使用 `React.StrictMode`
- 开发环境下副作用/渲染行为会被额外触发用于检查问题
- 这不会是生产根因，但会明显放大当前卡顿体感

---

## 影响范围评估

- 受影响页面：`PatchStudyPage` 主视图及侧栏（尤其 Chapters）
- 受影响交互：
  - 点击章节切换
  - 拖拽章节排序
  - 棋树编辑（增删改）后的 UI 刷新
  - 分析面板展示时的连续渲染

---

## 修复优先级建议（不改代码版）

## P0（先做）

1. 章节切换改为“**先本地选中，再异步加载树**”（乐观更新）
2. 清理或降级高频日志（至少去掉 render 内日志）
3. 避免 `studyContext` 中对整棵树反复 JSON 深拷贝

## P1（随后）

1. 拖拽 `dragover` 增加节流/去重（仅当 drop target 真变化时 setState）
2. 将 `ChapterList`、`MoveTree`、`StudyBoard` 做渲染隔离（memo + 稳定回调）

## P2（优化）

1. 样式层减少高成本阴影/过渡叠加
2. 评估虚拟列表（章节很多时）

---

## 备注

本诊断为静态代码排查结论，未进行运行时 profiler 录制。  
若需精确量化（每次点击耗时、JS Long Task 来源、重渲染次数），建议下一步补充 React Profiler + Performance 面板采样。
